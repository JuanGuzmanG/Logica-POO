{
    "files.autoSave": "afterDelay",
    "containers.commands.run": "${containerCommand} run --rm -d ${exposedPorts} ${tag}",
    "files.associations": {
        "*.embeddedhtml": "html",
        "iostream": "cpp"
    }
}